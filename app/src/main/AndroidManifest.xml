<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loading">
        <receiver
            android:name=".TodoWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.loading.TOGGLE_TODO" />
                <action android:name="com.example.loading.MOVE_TO_HISTORY" />
                <action android:name="com.example.loading.RESTORE_FROM_HISTORY" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <service
            android:name=".TodoWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Loading.BottomSheet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddTodoActivity"
            android:exported="false"
            android:label="添加待办"
            android:theme="@style/Theme.Loading.BottomSheet"
            android:windowSoftInputMode="adjustPan|stateVisible" />

        <activity
            android:name=".EditTodoActivity"
            android:exported="false"
            android:label="编辑待办"
            android:theme="@style/Theme.Loading.BottomSheet"
            android:windowSoftInputMode="adjustPan|stateVisible" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="设置"
            android:theme="@style/Theme.Loading.BottomSheet" />
    </application>

</manifest>
